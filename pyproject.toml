[project]
name = "bioquiet-backend"
version = "1.0.0"
description = "Proyecto de asig MAD en Kotlin"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.1.0",
]
